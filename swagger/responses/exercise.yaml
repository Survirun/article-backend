exercise_info:
  description: 운동 기록 1건
  content:
    application/json:
      schema:
        $ref: "../schemas/exercise.yaml#/exercise_res"
exercise_info_list:
  description: 운동 기록 목록
  content:
    application/json:
      schema:
        type: object
        properties:
          exerciseLog:
            type: array
            items:
              $ref: "../schemas/exercise.yaml#/exercise_res"
